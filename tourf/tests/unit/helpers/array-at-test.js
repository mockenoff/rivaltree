import { arrayAt } from '../../../helpers/array-at';
import { module, test } from 'qunit';

module('Unit | Helper | array at');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayAt(42);
  assert.ok(result);
});
