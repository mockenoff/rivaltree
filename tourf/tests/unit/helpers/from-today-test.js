import { fromToday } from '../../../helpers/from-today';
import { module, test } from 'qunit';

module('Unit | Helper | from today');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fromToday(42);
  assert.ok(result);
});
