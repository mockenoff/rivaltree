import { orCheck } from '../../../helpers/or-check';
import { module, test } from 'qunit';

module('Unit | Helper | or check');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = orCheck(42);
  assert.ok(result);
});
