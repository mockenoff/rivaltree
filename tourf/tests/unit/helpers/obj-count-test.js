import { objCount } from '../../../helpers/obj-count';
import { module, test } from 'qunit';

module('Unit | Helper | obj count');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = objCount(42);
  assert.ok(result);
});
