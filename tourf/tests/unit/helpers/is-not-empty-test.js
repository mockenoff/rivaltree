import { isNotEmpty } from '../../../helpers/is-not-empty';
import { module, test } from 'qunit';

module('Unit | Helper | is not empty');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isNotEmpty(42);
  assert.ok(result);
});
